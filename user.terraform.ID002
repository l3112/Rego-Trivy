$re = '[/^ca_[a-z0-9][a-z0-9][a-z0-9][a-z0-9]/m'];
#If a bracket inspects componets
# does that need to be in a bracket? 
#$str = 'ca_ap05';

#preg_match_all($re, $str, $matches, PREG_SET_ORDER, 0);

# Print the entire match result
